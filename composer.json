{
  "name": "ecoshot/site",
  "description": "One-page website (PL) + contact form via PHPMailer SMTP",
  "type": "project",
  "require": {
    "php": ">=8.0",
    "phpmailer/phpmailer": "^6.9"
  },
  "config": {
    "sort-packages": true
  }
}
